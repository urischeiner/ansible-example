---
plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  # All instances with their state as `running`
  instance-state-name: running

#---
#plugin: aws_ec2
#regions:
#  - us-east-1
#filters:
#  tag-key: Ansible
#  instance-state-name: running
